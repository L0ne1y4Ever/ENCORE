@startuml
skinparam shadowing false
skinparam DefaultFontName "Microsoft YaHei"
skinparam DefaultFontSize 13
title 图13  用户选座购票活动图

start
:用户登录系统;
:浏览演出列表;
:进入演出详情;
:选择具体场次;
:加载场次座位图;
note right: schedule_seat 表查询
:查看座位状态 (AVAILABLE / LOCKED / SOLD / DISABLED);
:选择一个或多个 AVAILABLE 座位;
if (座位是否仍可售?) then (是)
  :Redis 临时锁定座位\n (AVAILABLE -> LOCKED);
  :创建订单 (PENDING_PAYMENT);
  :启动订单倒计时 (默认 15 分钟);
  :跳转到支付页面;
  if (限定时间内完成支付?) then (是)
    :座位 SOLD;
    :订单 PAID;
    :生成电子票 (UNUSED);
    stop
  else (否)
    :订单 EXPIRED;
    :释放座位锁 -> AVAILABLE;
    stop
  endif
else (否)
  :提示座位已被他人锁定 / 售出;
  :返回选座页;
  stop
endif
@enduml
