@startuml
skinparam shadowing false
skinparam DefaultFontName "Microsoft YaHei"
skinparam DefaultFontSize 13
title 图25  剧目与场次管理活动图

|票务管理员|
start
:登录后台;
:维护场馆 venue;
:维护演出厅 venue_hall\n(类型 / 容量 / 清场缓冲);
:设计座位布局 seat_layout\n(区域 area + 座位 seat, SEATED/ZONED/MIXED);
:发布布局 (status=PUBLISHED);
:创建剧目信息\n(标题 / 简介 / 时长 / 分类 / 票价区间);
:创建演出场次\n(绑定剧目 + 厅/布局 + 时间 + 模式);
if (绑定了已发布布局?) then (是)
  :快照布局 ->\nschedule_seat + schedule_area_inventory;
else (否, SEATED 默认)
  :按行列生成默认座位池;
endif
:校验同厅时间冲突 (清场缓冲);
:设置发布与售票状态;
if (发布且开放?) then (是)
  :publishStatus=PUBLISHED, status=ON_SALE;
  stop
else (否)
  :保持 DRAFT / PREPARING;
  stop
endif
@enduml
