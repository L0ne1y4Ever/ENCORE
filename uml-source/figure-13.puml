@startuml
skinparam shadowing false
skinparam DefaultFontName "Microsoft YaHei"
skinparam DefaultFontSize 13
title 图25  剧目与场次管理活动图

|票务管理员|
start
:登录后台;
:维护演出分类;
:创建剧目信息\n(海报 / 简介 / 时长 / 票价区间);
:绑定演出分类;
:维护演出厅信息;
:维护演出厅座位布局\n(行号 / 列号 / 区域 / 是否可售);
:为剧目创建演出场次\n(选择剧目 + 演出厅 + 时间);
:设置场次票价分区;
if (演出厅座位布局完整?) then (是)
  :一键生成场次座位票池\n(schedule_seat 全量 AVAILABLE);
  :场次状态 DRAFT;
  if (审核通过?) then (是)
    :开放售票 (DRAFT -> ON_SALE);
    stop
  else (否)
    :停留 DRAFT;
    stop
  endif
else (否)
  :提示需要先完善座位布局;
  stop
endif
@enduml
