# ENCORE 开发日志 - 2026-05-14

## 今日目标

- 落地 GitHub 推送与项目记忆工作流。
- 建立后续新对话可恢复上下文的仓库文档。
- 补齐答辩友好的每日开发日志模板与提交索引。
- 验证当前前端工程仍可构建。

## 完成内容

- 确认 `D:\ENCORE` 已是 Git 仓库，当前分支为 `main`。
- 确认 GitHub remote 为 `origin -> https://github.com/L0ne1y4Ever/ENCORE.git`。
- 新增 `.gitignore`，避免后续误提交依赖、构建产物、日志和本地密钥。
- 新增 `PROJECT_MEMORY.md`，记录项目目标、技术栈、当前状态、账号、开发节奏和下一步任务。
- 新增 `DEVELOPMENT_PLAN.md`，按课程验收版本拆分后续开发阶段。
- 新增 `docs/commit-history.md`，用于答辩整理提交记录。
- 新增 `docs/demo-checklist.md`，整理最终演示路径。
- 新增本日日志文件，固定后续每日记录格式。
- 新增 `encore-backend` Spring Boot 3 后端工程。
- 配置 Maven、Java 17、MyBatis-Plus、Sa-Token、Redis、MySQL、Knife4j。
- 新增统一响应结构 `{ code, msg, data }` 与全局异常处理。
- 新增健康检查、登录/登出/当前用户、演出列表/详情/场次列表 API。
- 新增 `user_account`、`encore_show`、`show_schedule` 的实体、Mapper 和 Service。
- 新增 MySQL schema/seed SQL，种子数据与当前前端 mock 账号和演出数据保持一致。
- 新增根目录 `docker-compose.yml`，准备 MySQL 8.4 与 Redis 7.4 服务。
- 调整 `.gitignore`，避免本地助手状态、Python 缓存、工具 jar 和临时文档进入 GitHub。
- Docker Desktop 启动后，完成 MySQL/Redis 容器启动与健康检查。
- 将 MySQL 宿主机默认端口调整为 `3307`，避免与本机 MySQL 3306 冲突；云服务器容器内访问仍使用 `mysql:3306`。
- 完成登录、当前用户、演出列表、演出详情、场次列表接口的真实数据库调用验证。
- 记录项目最终部署目标为云服务器，后续配置优先使用 Docker Compose 与环境变量。

## 关键实现

- 项目记忆采用仓库文件方案，不依赖平台级跨对话记忆。
- 后续每次开发前默认读取 `PROJECT_MEMORY.md`、`DEVELOPMENT_PLAN.md` 和最新日志。
- 后续每完成一个可运行闭环，执行验证、更新日志、提交并推送到 `origin/main`。
- 后端第一批 API 保持 `/api/**` 前缀，返回统一结构，便于前端 Axios 后续统一拦截。
- Sa-Token 当前保留 Redis 集成，后续登录态可直接扩展到分布式部署。
- Docker Compose 只提交服务定义和初始化脚本，不提交本地 `.env`、数据卷或运行日志。

## 问题与解决

- 问题：此前规划中认为远程仓库尚未提供，但实际检查发现 GitHub remote 已经配置。
- 解决：直接沿用当前 `origin`，不重新初始化仓库，也不改变已有提交历史。
- 问题：新对话无法保证自动携带长期上下文。
- 解决：将上下文写入 `PROJECT_MEMORY.md`，并约定新会话先读取该文件。
- 问题：Docker Desktop 起初未运行，`docker compose up -d mysql redis` 无法连接 Docker API。
- 解决：Docker Desktop 启动后重新执行 Compose，MySQL/Redis 均健康。
- 问题：本机 3306 存在 MySQL 服务，但默认 `encore/encore123` 和 `root/root123` 无法登录。
- 解决：未擅自修改本机数据库权限，将项目 MySQL 宿主机映射端口改为 `3307`，后端 dev 默认端口同步改为 `3307`。

## 验证结果

- 已执行：`npm run build`。
- 结果：Vue TypeScript 检查和 Vite 生产构建通过。
- 备注：构建输出提示 Sass `@import` 弃用和部分 chunk 超过 500 kB，这是现有工程优化项，不阻塞本轮提交。
- 已执行：`mvn test` in `encore-backend`。
- 结果：后端编译通过，当前无测试用例。
- 已执行：启动后端并请求 `GET http://localhost:8080/api/health`。
- 结果：返回 `{"code":0,"msg":"ok","data":{"status":"UP","service":"encore-backend",...}}`。
- 已执行：`docker compose up -d mysql redis`。
- 结果：MySQL 映射到 `127.0.0.1:3307`，Redis 映射到 `127.0.0.1:6379`，两者健康检查通过。
- 已执行：MySQL seed 数据检查。
- 结果：`user_account=4`、`encore_show=4`、`show_schedule=5`。
- 已执行：登录、当前用户、演出列表、演出详情、场次列表 API 实调。
- 结果：`loginCode=0`、`currentUser=user`、`showCount=4`、`detailTitle=THE PHANTOM OF THE OPERA`、`scheduleCount=2`。
- 已提交：`.gitignore` 清理、后端基础工程、Docker Compose 和文档状态更新。

## GitHub 记录

- Commit: `abf2489 docs: add project memory and development workflow`
- Commit: `230942e docs: record workflow foundation commit`
- Commit: `58ce69a feat: add backend foundation`
- Branch: `main`
- Push 状态：已推送到 `origin/main`
- 本轮后端基础工程已完成实调和提交，推送因当前环境缺少 GitHub HTTPS 凭据而暂停。

## 明日计划

- 为前端添加 Axios 客户端并替换 auth/show mock API。
- 保留当前 mock 兜底或明确开发配置，降低答辩演示时环境故障风险。
- 开始 Phase 2：前端登录与演出浏览接入真实后端。
