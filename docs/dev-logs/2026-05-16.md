# ENCORE 开发日志 - 2026-05-16

## 今日目标

- 基于现有未提交前端优化继续开发。
- 稳定登录页、首页、个人页、后台新增页面相关的运行风险。
- 推进 Phase 4：实现后端检票核销接口并替换前端模拟检票逻辑。

## 完成内容

- 审阅当前未提交改动，确认主要集中在登录页、首页、个人页、后台场次/订单页和多语言文案。
- 使用 UI/UX 检查约束重新审视当前黑金票务界面，保留现有风格并避免破坏用户已做的页面主体。
- 登录页语言切换改为调用统一 `setAppLocale`，继续持久化语言偏好并同步 `html lang` / `data-locale`。
- 前端 Axios `requestData` 增强了错误解包，后端返回非 2xx 且包含统一响应体时会抛出业务消息。
- 恢复订单、支付、电子票、座位、后台演出管理、语言切换等页面仍在使用的中英文 i18n 键。
- 新增后端 DTO：`CheckInRequest`、`CheckInResponse`。
- 新增后端 `CheckInController`，提供 `POST /api/checkin/verify`。
- 新增后端 `CheckInService`，按票码查询票项并执行一次性核销。
- 检票接口限制 `checker`、`admin`、`sysadmin` 角色使用。
- 有效票据要求订单为 `PAID` 且票状态为 `UNUSED`，核销后更新为 `CHECKED_IN`。
- 重复核销、未支付/失效订单、无效票码、非 UNUSED 状态都会返回业务错误。
- 新增前端 `api/checkin.ts`。
- 检票页从本地 `XYZ` 后缀模拟逻辑切换为调用后端检票接口，并显示核销成功的演出、剧场和座位信息。
- 新增 `CheckInServiceTest`，覆盖成功核销、重复核销拒绝、普通用户无权限。
- 新增后端 `AdminController` / `AdminService`。
- 新增后台场次接口：场次列表、场次状态更新。
- 新增后台订单接口：订单列表、退款、强制核销。
- 后台场次页从 mock 数据切换到 `/api/admin/schedules`，支持刷新和状态流转。
- 后台订单页从 mock 数据切换到 `/api/admin/orders`，支持刷新、退款和强制核销。
- 后台操作加入加载态、禁用态、成功提示和后端错误提示。
- 新增后台演出 DTO：`AdminShowResponse`、`CreateShowRequest`、`UpdateShowRequest`、`UpdateShowStatusRequest`。
- 新增后台演出接口：全部状态列表、创建草稿、编辑基础字段、发布/下架/归档、软删除。
- 后台演出删除采用 `ARCHIVED` 软删除，保留已有场次、订单和票据关联。
- 后台演出列表返回 `scheduleCount`，便于管理员看到每个演出的场次数。
- 后台演出页从公开 `/api/shows` 改为 `/api/admin/shows`，新增 Element Plus 弹窗表单和真实 CRUD 操作。
- 后台演出操作加入 loading、disabled、成功提示、确认弹窗和后端错误提示。
- 补齐后台演出 CRUD 的中英文 i18n 文案。

## 关键实现

- 票码仍沿用 Phase 3 支付成功后生成的 `ticket_item.ticket_code`。
- 当前数据库表未新增 `checked_in_at` 字段，核销时间通过 `ticket_item.updated_at` 和接口响应中的 `checkedInAt` 表达。
- 检票服务优先校验角色，避免普通用户通过接口试探票码。
- 检票页保留极简大屏扫描视觉，但增加明确的提交按钮和加载状态，键盘回车仍可核销。
- 后台接口只允许 `admin` 和 `sysadmin` 使用。
- 订单退款会拒绝已核销订单；未核销的已支付订单退款后会将票项置为 `VOID`，并把对应 SOLD 座位释放回 AVAILABLE。
- 后台强制核销只处理已支付订单里的 `UNUSED` 票项，不改变订单支付状态。
- 演出 ID 由后端生成，格式为 `s-` 加 12 位 UUID 片段。
- 新增演出未传状态时默认 `DRAFT`，未传排序时使用当前最大 `sort_order + 10`。
- 演出状态限制为 `DRAFT`、`PUBLISHED`、`ARCHIVED`。
- 公开 `GET /api/shows` 保持只返回 `PUBLISHED`，后台 `GET /api/admin/shows` 返回全部状态。

## 问题与解决

- 问题：本轮 UI 改动删除了多个仍在使用的 i18n 键，构建不会失败但运行时会显示缺失文案。
- 解决：恢复所有核心演示页面仍引用的中英文键，避免订单/支付/电子票/后台页面出现键名。
- 问题：本轮前半段 Docker Desktop 未运行，`docker compose ps` 无法连接 Docker API，8080 后端也未处于可用状态。
- 解决：Docker Desktop 恢复后启动 MySQL/Redis 和 Spring Boot 后端，完成真实购票与检票链路实调。
- 问题：MyBatis-Plus `updateById` 在测试中存在单对象和集合重载，Mockito 断言参数类型不明确。
- 解决：在测试里显式使用 `TicketItem` 类型匹配器。
- 问题：浏览器插件无法直接 `fill` 登录输入框，报虚拟剪贴板不可用。
- 解决：改用逐键 `press` 完成登录，并验证后台演出页面和新增弹窗可见。

## 验证结果

- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，`CheckInServiceTest` 3 个用例全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：启动 Vite 开发服务器 `http://127.0.0.1:5173/`。
- 结果：启动成功。
- 已执行：浏览器打开 `http://127.0.0.1:5173/login`。
- 结果：登录页 hero 和语言按钮可见，控制台无错误。
- 已执行：`docker compose up -d mysql redis`。
- 结果：MySQL/Redis 均为 healthy，端口分别为 `3307` 和 `6379`。
- 已执行：启动 Spring Boot 后端并请求 `GET /api/health`。
- 结果：返回 `code:0`，服务为 `UP`。
- 已执行：真实 API 购票与检票脚本。
- 结果：`user/123` 登录，选择 `sch-101` 两个可售座位，锁座成功，创建订单 `ord-849a1a643a234feb`，支付后订单为 `PAID`，生成 2 张票；`checker/123` 核销 `TMP864LFX1-5XYZ` 后状态为 `CHECKED_IN`，重复核销返回业务码 `409` 和“票据已核销，不能重复入场”。
- 已执行：浏览器检票页验证。
- 结果：`checker/123` 登录后进入 `/checkin`，输入 `TMP864LFX1-6XYZ` 显示“核销通过”、演出名和座位；再次输入同一票码显示“票据已核销，不能重复入场”，控制台无错误。
- 已执行：真实后台 API 验证。
- 结果：`admin/123` 登录后读取 5 个场次；`sch-401` 从 `COMING_SOON` 临时切到 `PREPARING` 后恢复；新建两笔 `sch-102` 已支付订单，其中 `ord-76cc6822eab8459a` 强制核销后票据为 `1/1`，`ord-64c5157959364aa8` 退款后订单为 `REFUNDED`。
- 已执行：真实后台演出 CRUD API 验证。
- 结果：`admin/123` 创建 `s-21155dbbc804`，默认 `DRAFT`；编辑标题/分类成功；发布后公开 `/api/shows` 可见；下架回 `DRAFT` 后公开列表不可见；删除后状态为 `ARCHIVED` 且公开列表不可见；`user/123` 访问 `/api/admin/shows` 被拒绝，返回业务码 `401`。
- 已执行：浏览器后台演出页验证。
- 结果：通过浏览器进入 `http://localhost:5173/admin/shows`，后台演出管理表格、刷新/新增按钮、场次数列可见；新增演出弹窗可打开，副标题、封面 URL、状态、保存等字段可见。

## GitHub 记录

- Commit: `87e7fd0 feat: add ticket check-in flow`
- Commit: `f2d4a9d feat: connect admin operations to backend`
- Commit: `267f6cc feat: add admin show management CRUD`
- 当前工作区包含本轮后台演出 CRUD 接入和文档更新，已完成构建、测试、真实 API、浏览器页面验证。

## 明日计划

- 增加后台场次 CRUD 和座位池生成入口。
- 增加后台页面浏览器截图证据。
- 设计并实现检票时间窗口或场次约束。
