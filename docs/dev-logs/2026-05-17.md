# ENCORE 开发日志 - 2026-05-17

## 今日目标

- 继续 Phase 4 检票约束。
- 为普通检票接口增加场次时间窗口、取消场次和缺失场次校验。
- 保持后台强制核销作为管理员纠错能力，不受普通检票窗口限制。
- 启动 Phase 5 WebSocket 实时座位同步差异化功能。
- 接入后台 Dashboard WebSocket 自动刷新事件。

## 完成内容

- 新增后端 `TimeConfig`，提供生产环境默认 `Clock.systemDefaultZone()`。
- `CheckInService` 改为注入 `Clock`，便于固定时间做可重复单测。
- 普通 `POST /api/checkin/verify` 增加场次有效性校验：
  - 场次不存在时拒绝检票。
  - 场次为 `CANCELLED` 时拒绝检票。
  - 当前时间早于开演前 2 小时时拒绝检票。
  - 当前时间晚于场次结束时间时拒绝检票。
- 检票窗口边界为闭区间：`startTime - 2 hours` 和 `endTime` 当刻都允许核销。
- 保持后台 `/api/admin/orders/{id}/force-checkin` 原有逻辑，不套用普通检票窗口。
- 扩展 `CheckInServiceTest` 至 9 个用例，覆盖窗口内、两个边界、过早、过晚、取消场次、缺失场次、重复核销和无权限。
- 新增 `docs/demo-evidence/2026-05-17-purchase-flow.md`。
- 新增完整用户购票流程截图证据，覆盖登录、首页、演出详情、选座、订单确认、支付和电子票。
- 新增 Spring WebSocket + STOMP，开放 `/ws` 端点和按场次划分的 `/topic/schedules/{scheduleId}/seats` 主题。
- 新增座位状态事件广播，覆盖锁座、支付售出、订单过期释放、退款恢复可售、取消场次刷新。
- 前端选座页接入实时订阅，支持连接状态提示、原地更新座位状态、失效选择自动清理和取消场次刷新。
- 新增 `SeatServiceTest`、`OrderServiceTest`、`AdminServiceTest`，覆盖 `LOCKED`、`SOLD`、`EXPIRED -> AVAILABLE`、`REFUNDED -> AVAILABLE` 事件发布。
- 新增后台 Dashboard 刷新事件主题 `/topic/admin/dashboard`。
- 支付、订单过期、退款、后台强制核销、普通检票和演出状态变化会触发 Dashboard 刷新事件。
- 前端后台 Dashboard 接入实时刷新订阅，保留手动刷新按钮，并增加连接状态和自动刷新反馈。
- 新增 `DashboardRefreshPublisherTest`，并扩展订单、退款、检票测试覆盖刷新事件发布。

## 关键实现

- 时间窗口为开演前 2 小时到演出结束，直接使用现有 `show_schedule.start_time` / `end_time`，不新增数据库字段。
- 检票服务在订单支付状态和票据状态通过后，再读取场次并执行窗口校验；失败时不会更新 `ticket_item`。
- 后端错误仍使用中文业务消息，由现有前端错误处理直接显示到检票页。
- 本轮不修改 `CheckInRequest` / `CheckInResponse`，也不增加检票页场次选择。
- 浏览器证据使用真实后端链路，订单和票据保留在本地数据库作为演示数据。
- WebSocket 只负责广播展示态，锁座、下单、支付等写操作仍全部走现有受保护 HTTP API。
- 实时事件基于最终业务状态发布；支付成功只广播 `SOLD`，不会因为 Redis 解锁动作误报 `AVAILABLE`。
- Dashboard WebSocket 只发送轻量刷新事件，不传聚合指标；页面收到事件后复用现有 `/api/admin/dashboard` 拉取真实指标。

## 问题与解决

- 问题：如果直接使用 `LocalDateTime.now()`，时间窗口单测会随系统时间漂移。
- 解决：通过 Spring 注入 `Clock`，单测使用固定时间源验证窗口边界和拒绝路径。
- 问题：当前检票接口只传票码，没有检票台当前场次上下文。
- 解决：本轮按已确认范围只做时间、取消和缺失场次校验；真正的“检票台绑定当前场次”留到后续需要时再加。
- 问题：如果把 Redis 解锁动作直接映射为“可售”，支付完成时会先把座位错误广播为 `AVAILABLE`。
- 解决：将事件发布放在订单/后台业务结果处，由最终状态决定广播内容；底层 `releaseLocks()` 保持静默。
- 问题：启动验证时 8080 仍被旧 Java 进程占用，真实 STOMP 脚本一开始连到了旧后端。
- 解决：通过 `netstat` 定位旧 PID，停止旧后端和旧 Vite 进程后，用当前源码重启服务再验证。

## 验证结果

- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，`CheckInServiceTest` 9 个用例全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：重启 Spring Boot 后端并请求 `GET /api/health`。
- 结果：返回 `code:0`，服务为 `UP`。
- 已执行：真实 API 检票时间窗口回归。
- 结果：`user/123` 创建并支付未来场次 `sch-101` 的订单 `ord-01bd00f5cf3a4b09`；`checker/123` 核销票码 `TMP8K8PI91-7XYZ` 被拒绝，返回业务码 `409` 和“未到检票时间，开演前 2 小时开放检票”；`admin/123` 对同一订单执行强制核销成功，订单保持 `PAID` 且 `checkedInCount` 为 `1/1`。
- 已执行：完整用户购票浏览器截图验证。
- 结果：使用 `user/123` 从登录页进入首页，打开 `THE PHANTOM OF THE OPERA`，选择 `sch-101` 可售座位 `seat-1-9`，创建订单 `ord-1e911c2c5bba466d`，完成模拟支付后进入电子票页，票码为 `TMP8LC0P51-9XYZ`；7 张截图已保存到 `docs/demo-evidence/assets/2026-05-17-purchase-flow/`。
- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，当前后端共 13 个单测全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：真实 STOMP/API 联调。
- 结果：订阅 `/topic/schedules/sch-101/seats` 后，`user/123` 锁定 `seat-1-10` 收到 `LOCKED`，支付订单 `ord-110e7f3b7d824472` 后收到 `SOLD`，`admin/123` 退款后收到 `REFUNDED -> AVAILABLE`。
- 已执行：两个独立浏览器会话的实时座位同步验证。
- 结果：两个会话同时打开 `sch-101` 选座页，会话 A 锁座并支付后，会话 B 无刷新看到锁定数 `0 -> 1`、售出数 `11 -> 12`。
- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，当前后端共 14 个单测全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：真实 Dashboard STOMP/API 联调。
- 结果：订阅 `/topic/admin/dashboard` 后，支付订单 `ord-6df931f7f87e4685` 收到 `ORDER_PAID`，退款后收到 `ORDER_REFUNDED`。
- 已执行：浏览器后台 Dashboard 自动刷新验证。
- 结果：登录 `admin/123` 打开 `/admin` 后，页面显示“看板实时刷新中”；触发支付和退款事件后，Dashboard API 请求数从 `1` 增加到 `3`，页面显示“检测到数据变化”。

## GitHub 记录

- Commit: `6b0802d feat: add check-in time window validation`
- 当前工作区包含本轮提交索引记录更新，功能代码已完成测试、构建和真实 API 验证。
- Commit: `b5e5a27 docs: add purchase flow evidence`
- 当前工作区包含本轮购票流程证据提交索引记录更新，截图和证据文档已提交。
- Commit: `6b5c929 feat: add realtime seat updates`
- 本轮 WebSocket 功能已完成测试、构建、真实 STOMP/API 验证，并已推送到 `origin/main`。

## 明日计划

- 如答辩希望继续强化可视化，接入 three.js 座位舞台预览。
- 如答辩需要，增加检票台绑定当前场次后的错场次票据拒绝。
