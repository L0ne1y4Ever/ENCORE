# ENCORE 开发日志 - 2026-05-17

## 今日目标

- 继续 Phase 4 检票约束。
- 为普通检票接口增加场次时间窗口、取消场次和缺失场次校验。
- 保持后台强制核销作为管理员纠错能力，不受普通检票窗口限制。
- 启动 Phase 5 WebSocket 实时座位同步差异化功能。
- 接入后台 Dashboard WebSocket 自动刷新事件。
- 补齐检票台当前场次绑定，拒绝错场次票据。
- 接入 Three.js 座位舞台预览，增强用户选座页的可视化演示效果。
- 接入用户首页 Top 8 推荐区，使用真实后端数据展示热门和可售优先演出。
- 接入基础多人拼座邀请，使用 Redis 临时会话和发起人统一结算。
- 启动 Phase 6 交付包整理，补齐启动指南、用户手册、测试报告和完整 Compose 预览配置。

## 完成内容

- 新增后端 `TimeConfig`，提供生产环境默认 `Clock.systemDefaultZone()`。
- `CheckInService` 改为注入 `Clock`，便于固定时间做可重复单测。
- 普通 `POST /api/checkin/verify` 增加场次有效性校验：
  - 场次不存在时拒绝检票。
  - 场次为 `CANCELLED` 时拒绝检票。
  - 当前时间早于开演前 2 小时时拒绝检票。
  - 当前时间晚于场次结束时间时拒绝检票。
- 检票窗口边界为闭区间：`startTime - 2 hours` 和 `endTime` 当刻都允许核销。
- 保持后台 `/api/admin/orders/{id}/force-checkin` 原有逻辑，不套用普通检票窗口。
- 扩展 `CheckInServiceTest` 至 9 个用例，覆盖窗口内、两个边界、过早、过晚、取消场次、缺失场次、重复核销和无权限。
- 新增 `docs/demo-evidence/2026-05-17-purchase-flow.md`。
- 新增完整用户购票流程截图证据，覆盖登录、首页、演出详情、选座、订单确认、支付和电子票。
- 新增 Spring WebSocket + STOMP，开放 `/ws` 端点和按场次划分的 `/topic/schedules/{scheduleId}/seats` 主题。
- 新增座位状态事件广播，覆盖锁座、支付售出、订单过期释放、退款恢复可售、取消场次刷新。
- 前端选座页接入实时订阅，支持连接状态提示、原地更新座位状态、失效选择自动清理和取消场次刷新。
- 新增 `SeatServiceTest`、`OrderServiceTest`、`AdminServiceTest`，覆盖 `LOCKED`、`SOLD`、`EXPIRED -> AVAILABLE`、`REFUNDED -> AVAILABLE` 事件发布。
- 新增后台 Dashboard 刷新事件主题 `/topic/admin/dashboard`。
- 支付、订单过期、退款、后台强制核销、普通检票和演出状态变化会触发 Dashboard 刷新事件。
- 前端后台 Dashboard 接入实时刷新订阅，保留手动刷新按钮，并增加连接状态和自动刷新反馈。
- 新增 `DashboardRefreshPublisherTest`，并扩展订单、退款、检票测试覆盖刷新事件发布。
- `CheckInRequest` 新增可选 `scheduleId`，旧脚本只传 `ticketCode` 时继续兼容。
- `POST /api/checkin/verify` 在绑定当前场次时会先校验该场次存在、未取消且处于检票窗口，再要求票据属于该场次。
- 新增 `GET /api/checkin/schedules`，允许 `checker/admin/sysadmin` 获取非取消场次，并优先展示当前可检票场次。
- 检票页新增当前场次选择控件，选择值保存到 `localStorage`，核销请求会携带当前 `scheduleId`。
- 错场次票据固定返回“票据不属于当前检票场次”；后台订单强制核销仍不套用检票台绑定约束。
- 新增 `SeatStagePreview` 独立组件，在现有 2D 座位图上方渲染 Three.js 舞台和座位池。
- 3D 预览直接复用现有 `Seat` 数据，不新增后端接口或数据库字段。
- 3D 座位按 `AVAILABLE / LOCKED / SOLD / DISABLED / selected` 显示状态；点击可售 3D 座位复用现有 `toggleSeat` 逻辑。
- 保留 2D 座位图和右侧结算栏作为主购票路径，3D 预览作为增强交互和答辩亮点。
- WebSocket 座位事件更新同一份 `seats` 状态，3D 预览与 2D 座位图同步变化。
- 新增公开 `GET /api/shows/recommendations/top8` 推荐接口。
- 推荐接口只返回 `PUBLISHED` 演出，并按可售场次优先、有效付费票数、已支付收入、排序值和创建时间排名。
- 新增 `ShowRecommendationResponse`，在基础演出字段上补充 `rank`、`ticketsSold`、`availableScheduleCount`、`hotScore`。
- 扩展 `ShowServiceTest`，覆盖可售优先、票数排序、收入排序、未发布过滤、无效订单/票据排除、无销量排序兜底和 Top 8 限制。
- 首页在 hero 下方新增 Top 8 推荐横向区，展示排名、售票数和可售场次数，点击后进入现有演出详情。
- 推荐接口失败时降级使用公开演出列表前 8 个，不阻塞首页浏览和分类筛选。
- 新增 Redis 临时拼座会话，key 为 `encore:group-order:{inviteCode}`，TTL 固定 15 分钟。
- 新增拼座接口：创建、读取、加入/更新、退出、取消和发起人结算。
- 拼座座位锁 owner 使用 `group:{inviteCode}`；发起人结算时把锁 owner 转为真实订单号，然后继续复用现有支付、售出和电子票流程。
- 前端选座页支持 `?group={inviteCode}` 拼座模式，显示邀请链接、成员列表、倒计时、当前认领座位、拼座合计、加入/退出/取消/结算操作。
- 普通选座模式新增“发起拼座”按钮；拼座模式隐藏普通“去结算”，避免双路径购买。
- 新增普通用户种子账号 `friend / 123`，用于双浏览器拼座演示。
- 新增根目录 `README.md`，汇总本地启动、完整 Docker Compose 预览、演示账号和验证命令。
- 新增 `docker-compose.full.yml`，包含 MySQL、Redis、Spring Boot 后端和 Nginx 前端。
- 新增后端/前端 Dockerfile 与前端 Nginx 代理配置，容器模式下 `/api` 和 `/ws` 转发到后端服务。
- 新增 `docs/user-manual.md`，覆盖用户购票、拼座邀请、检票和后台管理演示流程。
- 新增 `docs/test-report.md`，记录自动化测试、手工验证矩阵和答辩证据建议。
- 更新前后端 README，使其指向根启动文档并说明组件级命令。

## 关键实现

- 时间窗口为开演前 2 小时到演出结束，直接使用现有 `show_schedule.start_time` / `end_time`，不新增数据库字段。
- 检票服务在订单支付状态和票据状态通过后，再读取场次并执行窗口校验；失败时不会更新 `ticket_item`。
- 后端错误仍使用中文业务消息，由现有前端错误处理直接显示到检票页。
- 时间窗口轮最初没有修改 `CheckInRequest` / `CheckInResponse`；当前场次绑定轮已在 `CheckInRequest` 中新增可选 `scheduleId`，`CheckInResponse` 保持不变。
- 浏览器证据使用真实后端链路，订单和票据保留在本地数据库作为演示数据。
- WebSocket 只负责广播展示态，锁座、下单、支付等写操作仍全部走现有受保护 HTTP API。
- 实时事件基于最终业务状态发布；支付成功只广播 `SOLD`，不会因为 Redis 解锁动作误报 `AVAILABLE`。
- Dashboard WebSocket 只发送轻量刷新事件，不传聚合指标；页面收到事件后复用现有 `/api/admin/dashboard` 拉取真实指标。
- 检票台当前场次绑定只保存在浏览器本地，不新增检票台实体或数据库表；后台强制核销继续作为运营纠错通道。
- Three.js 仅在前端选座页使用；浏览器不支持 WebGL 时显示 fallback 文案，不阻塞下方 2D 选座和 HTTP 锁座流程。
- 3D 预览保持无框剧场带状区域，不替代 2D 座位图，避免影响移动端和精确选座。
- 推荐热度直接从现有 `ticket_order`、`ticket_item`、`show_schedule`、`encore_show` 聚合，不新增表，也不引入个性化行为记录。
- 有效热度只统计 `PAID` 订单下 `UNUSED` / `CHECKED_IN` 票，退款、作废和未支付订单不计入推荐票数。
- 首页推荐区独立于分类 tab；分类筛选仍只控制下方常规演出列表。
- 拼座会话不新增数据库表，未结算会话随 Redis TTL 过期；结算后的订单和票据仍持久化到现有 `ticket_order` / `ticket_item`。
- 拼座支付采用发起人统一支付，不做成员拆单或各自支付。
- 拼座最多 6 个座位，沿用当前订单座位数上限。
- 默认 `docker-compose.yml` 仍只负责本地开发所需的 MySQL/Redis；完整容器预览使用单独的 `docker-compose.full.yml`，避免打断当前开发习惯。
- Nginx 前端镜像使用相对 API 基址 `/`，通过同源代理解决容器预览下的 API 与 WebSocket 访问。

## 问题与解决

- 问题：如果直接使用 `LocalDateTime.now()`，时间窗口单测会随系统时间漂移。
- 解决：通过 Spring 注入 `Clock`，单测使用固定时间源验证窗口边界和拒绝路径。
- 问题：当前检票接口只传票码，没有检票台当前场次上下文。
- 解决：先按已确认范围完成时间、取消和缺失场次校验；随后新增可选 `scheduleId` 和检票页当前场次绑定，保持旧调用兼容。
- 问题：如果把 Redis 解锁动作直接映射为“可售”，支付完成时会先把座位错误广播为 `AVAILABLE`。
- 解决：将事件发布放在订单/后台业务结果处，由最终状态决定广播内容；底层 `releaseLocks()` 保持静默。
- 问题：启动验证时 8080 仍被旧 Java 进程占用，真实 STOMP 脚本一开始连到了旧后端。
- 解决：通过 `netstat` 定位旧 PID，停止旧后端和旧 Vite 进程后，用当前源码重启服务再验证。

## 验证结果

- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，`CheckInServiceTest` 9 个用例全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：重启 Spring Boot 后端并请求 `GET /api/health`。
- 结果：返回 `code:0`，服务为 `UP`。
- 已执行：真实 API 检票时间窗口回归。
- 结果：`user/123` 创建并支付未来场次 `sch-101` 的订单 `ord-01bd00f5cf3a4b09`；`checker/123` 核销票码 `TMP8K8PI91-7XYZ` 被拒绝，返回业务码 `409` 和“未到检票时间，开演前 2 小时开放检票”；`admin/123` 对同一订单执行强制核销成功，订单保持 `PAID` 且 `checkedInCount` 为 `1/1`。
- 已执行：完整用户购票浏览器截图验证。
- 结果：使用 `user/123` 从登录页进入首页，打开 `THE PHANTOM OF THE OPERA`，选择 `sch-101` 可售座位 `seat-1-9`，创建订单 `ord-1e911c2c5bba466d`，完成模拟支付后进入电子票页，票码为 `TMP8LC0P51-9XYZ`；7 张截图已保存到 `docs/demo-evidence/assets/2026-05-17-purchase-flow/`。
- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，当前后端共 13 个单测全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：真实 STOMP/API 联调。
- 结果：订阅 `/topic/schedules/sch-101/seats` 后，`user/123` 锁定 `seat-1-10` 收到 `LOCKED`，支付订单 `ord-110e7f3b7d824472` 后收到 `SOLD`，`admin/123` 退款后收到 `REFUNDED -> AVAILABLE`。
- 已执行：两个独立浏览器会话的实时座位同步验证。
- 结果：两个会话同时打开 `sch-101` 选座页，会话 A 锁座并支付后，会话 B 无刷新看到锁定数 `0 -> 1`、售出数 `11 -> 12`。
- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，当前后端共 14 个单测全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：真实 Dashboard STOMP/API 联调。
- 结果：订阅 `/topic/admin/dashboard` 后，支付订单 `ord-6df931f7f87e4685` 收到 `ORDER_PAID`，退款后收到 `ORDER_REFUNDED`。
- 已执行：浏览器后台 Dashboard 自动刷新验证。
- 结果：登录 `admin/123` 打开 `/admin` 后，页面显示“看板实时刷新中”；触发支付和退款事件后，Dashboard API 请求数从 `1` 增加到 `3`，页面显示“检测到数据变化”。
- 已执行：`mvn -Dtest=CheckInServiceTest test` in `encore-backend`。
- 结果：通过，`CheckInServiceTest` 扩展到 18 个用例，覆盖绑定正确、错场次、绑定场次缺失/取消/未到窗口/已结束、旧调用兼容和场次列表权限。
- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，当前后端共 23 个单测全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：真实 API 检票台绑定验证。
- 结果：新建当前窗口内场次 `sch-d32a1eaf3ac2` / `sch-cdc83ac78ad8`；用户购买并支付订单 `ord-8b44aefd16224484`，票码 `TMP8PGYBF1-1XYZ`；绑定错误场次返回“票据不属于当前检票场次”，绑定正确场次后核销成功。
- 已执行：检票场次列表权限验证。
- 结果：`checker/123` 可读取 `/api/checkin/schedules`；`user/123` 被拒绝，返回 `401` 和“当前账号无检票权限”。
- 已执行：浏览器检票页验证。
- 结果：登录 `checker/123` 后进入 `/checkin`，场次下拉加载真实后端数据；选择值写入 `localStorage` 并在刷新后保持；核销请求 payload 包含当前 `scheduleId`。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；Three.js 选座页 chunk 触发既有 Vite 大 chunk 警告，Sass `@import` 弃用警告仍存在。
- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，当前后端共 23 个单测全部通过。
- 已执行：Three.js 选座页浏览器验证。
- 结果：登录 `user/123` 打开 `/seat/sch-101`，桌面 canvas 截图 `1024x325` 非暗像素 `125965`，移动 canvas 截图 `294x218` 非暗像素 `41868`；拖动视角后变化像素 `145393`。
- 已执行：3D 点击选座与购票回归。
- 结果：点击 3D 可售座位后右侧已选列表和 2D 座位选中态同步为 `1`；继续锁座进入 `/confirm`，`tempOrder` 为 `sch-101 / seat-7-6 / 100`。
- 已执行：WebSocket 事件驱动 3D 重绘验证。
- 结果：对 `sch-101` 锁定 `seat-1-11` 后页面收到“座位状态已更新”，3D canvas 变化像素 `175`。
- 已执行：`mvn -Dtest=ShowServiceTest test` in `encore-backend`。
- 结果：通过，`ShowServiceTest` 5 个用例覆盖 Top 8 推荐排序和过滤规则。
- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，当前后端共 28 个单测全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：真实推荐 API 验证。
- 结果：`GET /api/health` 返回 `code:0`；`GET /api/shows/recommendations/top8` 返回 `code:0` 和 4 个已发布演出，第一名为 `THE PHANTOM OF THE OPERA`，包含 `ticketsSold: 17`、`availableScheduleCount: 4`；普通 `GET /api/shows` 仍返回公开演出列表。
- 已执行：浏览器首页推荐区验证。
- 结果：登录 `user/123` 打开首页，页面请求 `/api/shows/recommendations/top8` 成功；Top 8 推荐区显示 4 张真实卡片，第一张展示“17 张有效票 / 4 个可售场次”，点击进入 `/show/s-001`。
- 已执行：`mvn -Dtest=GroupOrderServiceTest test` in `encore-backend`。
- 结果：通过，`GroupOrderServiceTest` 6 个用例覆盖创建、成员替换座位、超过 6 座拒绝、非发起人结算拒绝、缺失邀请码拒绝和发起人结算。
- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，当前后端共 34 个单测全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。
- 已执行：真实 API 拼座验证。
- 结果：`user/123` 创建拼座 `g-548bb9edf9`，座位 `seat-1-11`；`friend/123` 加入座位 `seat-1-12`；发起人结算生成订单 `ord-b805ecb4af2c49c3`，模拟支付成功，订单状态 `PAID`，生成 2 张票 `TMP9FP9R51-11XYZ,TMP9FP9R51-12XYZ`。
- 已执行：双浏览器拼座 UI 验证。
- 结果：`user/123` 在 `/seat/sch-101` 发起拼座 `g-5b7f36d8e9`；`friend/123` 打开邀请链接加入座位；发起人页面轮询显示 2 位成员和 `$300` 拼座合计；发起人结算进入 `/payment?id=ord-8724105db9d64186`，支付后进入 `/ticket/ord-8724105db9d64186`。
- 已执行：`docker compose -f docker-compose.full.yml config`。
- 结果：通过，完整 Compose 配置可解析，包含 frontend/backend/mysql/redis 服务和数据卷。
- 已执行：`mvn test` in `encore-backend`。
- 结果：通过，当前后端共 34 个单测全部通过。
- 已执行：`npm run build` in `encore-frontend`。
- 结果：通过；仍有既有 Sass `@import` 弃用和大 chunk 警告。

## GitHub 记录

- Commit: `6b0802d feat: add check-in time window validation`
- 当前工作区包含本轮提交索引记录更新，功能代码已完成测试、构建和真实 API 验证。
- Commit: `b5e5a27 docs: add purchase flow evidence`
- 当前工作区包含本轮购票流程证据提交索引记录更新，截图和证据文档已提交。
- Commit: `6b5c929 feat: add realtime seat updates`
- 本轮 WebSocket 功能已完成测试、构建、真实 STOMP/API 验证，并已推送到 `origin/main`。
- Commit: `13f73f4 feat: add dashboard realtime refresh`
- 本轮 Dashboard WebSocket 刷新功能已完成测试、构建、真实 STOMP/API 验证和浏览器自动刷新验证，并已推送到 `origin/main`。
- Commit: `dd161b6 feat: bind check-in station schedules`
- 本轮检票台当前场次绑定已完成测试、构建、真实 API 验证和浏览器验证，并已推送到 `origin/main`。
- Commit: `91e5a0d feat: add 3d seat stage preview`
- 本轮 Three.js 座位舞台预览已完成构建、后端回归、桌面/移动浏览器像素检查、3D 选座、锁座确认页和 WebSocket 重绘验证，并已推送到 `origin/main`。
- Commit: `2212ef2 feat: add top show recommendations`
- 本轮 Top 8 推荐区已完成后端单测、完整后端回归、前端构建、真实 API 和浏览器首页验证，并已推送到 `origin/main`。
- Commit: `a55df93 feat: add group seat invitations`
- 本轮基础拼座邀请已完成后端单测、完整后端回归、前端构建、真实 API 和双浏览器 UI 验证，并已推送到 `origin/main`。
- Commit: current loop `docs: add delivery startup package`
- 本轮 Phase 6 交付启动包已完成根 README、用户手册、测试报告、完整 Compose 预览配置，并通过 Compose 配置校验、后端测试和前端构建。

## 明日计划

- 继续 Phase 6：补实时座位、Dashboard 刷新、拼座邀请的答辩截图或录屏证据。
- 视时间补云服务器部署变量说明和最终答辩摘要。
